name: "Morphata: Test and Deploy"

on:
  pull_request:
    branches: [main]
    paths:
      - 'packages/morphata/**'
      - '.github/workflows/morphata.yml'
  push:
    branches: [main]
    paths:
      - 'packages/morphata/**'
      - '.github/workflows/morphata.yml'
    tags:
      - morphata-v*

permissions:
  contents: read

jobs:
  test:
    name: Test morphata
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: uv sync --frozen --dev --all-packages
      - name: Format check
        working-directory: packages/morphata
        run: uv run --frozen ruff format --check src/ tests/
      - name: Lint
        working-directory: packages/morphata
        run: uv run --frozen ruff check src/ tests/
      - name: Test with Python ${{ matrix.python-version }}
        working-directory: packages/morphata
        run: |
          if [[ "${{ matrix.python-version }}" == "3.14" ]]; then
            uv run --frozen pytest tests/ -v -o addopts=""
          else
            uv run --frozen pytest tests/ -v
          fi
      - name: Type check
        working-directory: packages/morphata
        run: |
          uv run --frozen ty check --output-format concise src/ tests/
          uv run --frozen pyrefly check --output-format min-text src/ tests/

  release:
    name: Build and release morphata
    runs-on: ubuntu-latest
    needs: test
    if: startsWith(github.ref, 'refs/tags/morphata-v')
    permissions:
      id-token: write

    environment:
      name: pypi-morphata
      url: https://pypi.org/p/morphata

    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
      - name: Install dependencies
        run: uv sync --frozen
      - name: Build wheels and sdist
        run: uv build --package morphata
      - name: Publish to PyPI
        run: uv publish --package morphata
